@{
    const string root = @"~\Documentation";
    const string cfg = @"~\config\githubpull.config";
    
    var zip = new uDocumentation.Busineslogic.GithubSourcePull.ZipDownloader(HttpContext.Current.Server.MapPath(root), HttpContext.Current.Server.MapPath(cfg));
    zip.Run();
}